# 更新记录

## 2026-09-23：日志清空与控制台转发

- 持久保存清空时间，切页、切换标签和重启管理器不再恢复已清空日志。
- 按微秒时间续接并去重，保留独立管理器已有的有界缓冲和批量刷新。
- 增加脚本 console 标签，默认显示全部关键标签。
- 普通明文和加密脚本的控制台输出转发至 ZFM-Script，保留原控制台调用和用户 Toast。
- 正确处理异步加载失败，保留明文 Frida 模块包的原生加载路径。
- 将日志修复迁移到独立工程，保留多脚本配置、应用绑定和事务推送功能。

## 2026-09-22：1.0.0 首次发布

- 将当前独立管理器作为新的 Git 初始版本，移除旧提交历史对主分支的关联。
- 发布可直接安装的 release APK，包名 `re.zyg.fri.manager`，版本号 `1.0.0`（versionCode 1）。
- 提供多脚本配置与应用绑定、事务推送、运行状态、应用停止和日志筛选功能。
- APK 沿用项目当前的开发用 debug 签名；模块与 Gadget 仍由用户单独安装。

## 2026-09-22：独立管理器工程

- 将 Android 应用、Gradle Wrapper、文档与自测提升到仓库根目录，根工程仅构建 `:app`。
- 移除上游 ZygiskFrida 源码、模块打包模板及旧构建配置。
- 模块与 Gadget 作为外部运行时依赖，仅记录下载来源和兼容基线，不随仓库或 APK 分发。
- 统一构建命令、文档链接和自测路径；保留应用包名、配置格式及历史许可证。
- 不配置 GitHub Actions，构建和测试在本地执行。

## 2026-09-21：第二轮视觉与排版优化

- 内置 Noto Sans SC 常规和中等字重，区分标题、正文、辅助信息与路径文字。
- 改用白灰底色、蓝色操作和绿色状态，统一细线图标、轻量输入框和按钮。
- 首页精简环境状态，将添加目标改为列表标题旁的加号入口。
- 将长说明、完整路径与锁定详情放入问号帮助弹窗，工具高级设置按需展开。
- 收紧目标编辑、脚本和日志排版，修正延时选项对齐与大字体按钮裁切。
- 更新中文使用手册，随应用附带字体许可证。

## 2026-09-21：功能分页与界面现代化

- 接入原生 Material 3 界面，增加「目标、脚本、日志、工具」底部菜单。
- 统一工具栏、表单、列表、对话框和日夜配色，增加应用图标、目标状态与更多操作入口。
- 改善目标编辑、延时选择、库路径、脚本模式切换和工具输出的布局。
- 分页切换保留当前草稿与输出，详情页保留清晰的返回路径。
- 修正脚本表单保存时未同步到 JSON 的行为，并保留同模式下的高级配置字段。
- 日志支持直接选择标签，离开页面暂停采集，停止时仅结束本次采集进程。
- 删除自动注入 Toast 的设置与生成代码，保留用户脚本自己的 Toast；明文脚本不再附加提示 loader。
- 升级管理器构建工具链，补充配置回归测试和新版中文操作说明。

详细说明见 [界面更新说明](docs/ui_update_zh.md)，使用流程见 [中文使用手册](docs/usage_zh.md)。
